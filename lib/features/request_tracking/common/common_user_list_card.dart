import 'package:flutter/material.dart';

class CommonUserDetailsCard extends StatelessWidget {
  const CommonUserDetailsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}